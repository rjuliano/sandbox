version: 1
backend:
  phases:
    preBuild:
      commands:
        - export BASE_PATH=$(pwd)
    build:
      commands:
        # - alternatives --install /usr/bin/python3 python3 /usr/local/bin/python3.8 10
        # - cd amplify/backend/function/numpypandasLayer/opt
        # - pip3.8 install -r requirements.txt --target=./python/lib/python3.8/site-packages
        # - pip3.8 install pipenv
        # - cd $BASE_PATH
        # - amplifyPush --simple
frontend:
  phases:
    preBuild:
      commands:
        - cd frontend
        - yarn install
    build:
      commands:
        - yarn run build
  artifacts:
    baseDirectory: frontend/.next
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*