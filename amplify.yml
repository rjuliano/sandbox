version: 1
applications:
  - frontend:
      phases:
        build:
          commands: 
          - npm run-script build
      artifacts:
        baseDirectory: /dist
        files:
          - '**/*'
      cache:
        paths: []
    appRoot: web
